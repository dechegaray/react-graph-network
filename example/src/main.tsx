import React from "react";
import ReactDOM from "react-dom";

import { MyNetworkGraph } from "./my-network-graph";

ReactDOM.render(
  <React.StrictMode>
    <MyNetworkGraph />
  </React.StrictMode>,
  document.getElementById("root")
);
